using System.Security.Cryptography.X509Certificates;

namespace lalala
{
    internal class Program
    {
        //struct Hrana
       // {
           // public int Start;
           // public int Cil; 
           // public int Len; 
            //public bool Placeni;

           // public Hrana(int Start,int Cil,int Len, int placena) 
                //{
                //Start = start;
               // Cil = cil;
               // Len = len;
                //if (placena == 1)
               // {
               //     Placeni = true;
                //}
               // else
               // {
                   // Placeni = false;
                //}

            //}
       // }
        static void Main(string[] args)
        {
           string blem = Console.ReadLine();
           string[] mlem = blem.Split();
            int vrch = Convert.ToInt32(mlem[0]);
            int hranc = Convert.ToInt32(mlem[1]);
            for(int i = 0; i < hranc; i++);
            {
                string udaj = Console.ReadLine();
                string[] plop = udaj.Split();
                int start = Convert.ToInt32(plop[0]);
                int cil
            }
        }
    }
}
